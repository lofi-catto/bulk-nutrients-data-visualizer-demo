{
  "name": "data-visualizer",
  "version": "1.0.0",
  "description": "NodeJs ExpressJs ReactJs",
  "main": "app.js",
  "scripts": {
    "start": "node app",
    "heroku-postbuild": "cd client && npm install && npm run build",
    "server": "nodemon",
    "client": "npm start --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "npm run build --prefix client"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "cross-fetch": "^3.1.4",
    "dotenv": "^16.0.0",
    "express": "^4.17.3"
  },
  "devDependencies": {
    "concurrently": "^6.0.2"
  }
}